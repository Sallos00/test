name: Build EXE with Cython Obfuscation

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    env:
      PYTHONUTF8: "1"
      AUTH_SERVER_URL: ${{ secrets.AUTH_SERVER_URL }}
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - name: 소스 체크아웃
        uses: actions/checkout@v4

      - name: Python 3.11 설치
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 의존성 설치
        run: |
          python -m pip install --upgrade pip
          pip install opencv-python numpy sounddevice scipy pywin32 comtypes psutil pyaudiowpatch pystray Pillow winotify mss pyinstaller Cython setuptools wheel

      - name: ffmpeg 다운로드 (번들용)
        run: |
          curl -L "https://github.com/BtbN/ffmpeg-builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip" -o ffmpeg.zip
          7z e ffmpeg.zip "*/bin/ffmpeg.exe" -o"." -r
          if not exist ffmpeg.exe (echo "ffmpeg 압축 해제 실패" && exit 1)
          echo "ffmpeg 준비 완료"
        shell: cmd

      - name: 아이콘 생성
        run: python scripts/make_icon.py

      - name: 버전 정보 생성
        run: python scripts/make_version.py

      - name: SERVER_URL 삽입
        run: python scripts/inject_url.py

      # ── Cython 컴파일 ───────────────────────────────────────────────────
      - name: Cython 컴파일 폴더 준비
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path build_cython | Out-Null
          Write-Host "✓ 컴파일 폴더 생성 완료"

      - name: Python 파일을 .pyx로 변환
        shell: pwsh
        run: |
          Write-Host "Python 파일을 Cython 형식으로 변환..."
          $files = @(
            "app_icon.py","audio_com.py","audio_capture.py",
            "log_utils.py","mem_utils.py","processes.py",
            "win32_utils.py","auth.py","db_manager.py",
            "video_hash.py","similarity.py","series_key.py","main.py"
          )
          foreach ($f in $files) {
            Copy-Item $f "build_cython\$($f -replace '\.py$', '.pyx')"
            Write-Host "  ✓ $f → $($f -replace '\.py$', '.pyx')"
          }
          Copy-Item gui build_cython\gui -Recurse
          Write-Host "✓ gui 패키지 복사 완료"

      - name: Cython 컴파일 (setup.py 사용)
        shell: pwsh
        run: |
          Write-Host "Cython으로 Python 파일 컴파일 중..."
          python setup_cython.py build_ext --inplace --build-temp build_cython
          if ($LASTEXITCODE -ne 0) {
            Write-Host "ERROR: Cython 컴파일 실패"
            exit $LASTEXITCODE
          }
          Write-Host "✓ Cython 컴파일 완료"

      - name: 컴파일된 파일 정리
        shell: pwsh
        run: |
          Write-Host "컴파일 결과물 확인..."
          Get-ChildItem -Path . -Filter "*.pyd" -Recurse | Format-Table FullName
          Get-ChildItem -Path build_cython -Recurse | Where-Object {$_.Extension -in @('.pyd','.so')} | Format-Table FullName
          Write-Host "✓ 정리 완료"

      # ── PyInstaller로 EXE 빌드 ───────────────────────────────────────────
      - name: PyInstaller EXE 빌드
        run: >
          pyinstaller
          --clean
          --onefile
          --noconsole
          --name "Auto Sinc"
          --icon "app.ico"
          --version-file "version_info.txt"
          --hidden-import="app_icon"
          --hidden-import="audio_com"
          --hidden-import="audio_capture"
          --hidden-import="log_utils"
          --hidden-import="mem_utils"
          --hidden-import="processes"
          --hidden-import="win32_utils"
          --hidden-import="auth"
          --hidden-import="db_manager"
          --hidden-import="video_hash"
          --hidden-import="similarity"
          --hidden-import="series_key"
          --hidden-import="gui"
          --hidden-import="gui.base"
          --hidden-import="gui.ui_layout"
          --hidden-import="gui.ui_logic"
          --hidden-import="gui.ui_logic2"
          --hidden-import="gui.ui_logic3"
          --hidden-import="gui.auth"
          --hidden-import="gui.run"
          --hidden-import="gui.run_oped"
          --hidden-import="gui.run_process"
          --hidden-import="gui.run_popup"
          --hidden-import="gui.run_notify"
          --hidden-import="gui.run_refresh"
          --hidden-import="gui.record_open"
          --hidden-import="gui.record_backend"
          --hidden-import="gui._record_impl"
          --hidden-import="cv2"
          --hidden-import="sounddevice"
          --hidden-import="scipy.signal"
          --hidden-import="win32api"
          --hidden-import="win32con"
          --hidden-import="win32gui"
          --hidden-import="tkinter"
          --hidden-import="tkinter.messagebox"
          --hidden-import="tkinter.filedialog"
          --hidden-import="multiprocessing"
          --hidden-import="comtypes"
          --hidden-import="comtypes.client"
          --hidden-import="winreg"
          --hidden-import="psutil"
          --hidden-import="mss"
          --hidden-import="pystray"
          --hidden-import="pystray._win32"
          --hidden-import="pystray._base"
          --hidden-import="pystray._util"
          --hidden-import="six"
          --hidden-import="PIL"
          --hidden-import="PIL.Image"
          --hidden-import="PIL._imaging"
          --collect-all PIL
          --copy-metadata Pillow
          --hidden-import="winotify"
          --hidden-import="urllib.request"
          --hidden-import="urllib.parse"
          --hidden-import="uuid"
          --hidden-import="hashlib"
          --hidden-import="platform"
          --collect-data cv2
          --add-data "lbpcascade_animeface.xml;."
          --add-data "app.ico;."
          --add-data "ffmpeg.exe;."
          main.py

      - name: 빌드 결과 확인
        run: dir dist\
        shell: cmd

      - name: EXE 업로드
        uses: actions/upload-artifact@v4
        with:
          name: AutoSync-EXE
          path: dist\Auto Sinc.exe
          retention-days: 3
